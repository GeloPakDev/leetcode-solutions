package com.leetsols.esm.arrays;

//TODO
public class ToeplitzMatrix {
    /*
     * Toeplitz if every diagonal
     * from top-left to bottom-right has the same elements
     */
    public boolean isToeplitzMatrix(int[][] matrix) {
        return true;
    }
}
